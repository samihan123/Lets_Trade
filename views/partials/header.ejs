<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- css file for formating -->
    <link rel='stylesheet' type='text/css' media='screen' href='/css/bootstrap.min.css'>
    <link rel='stylesheet' type='text/css' media='screen' href= <%= cssurl %>>

    <!-- brand ;logo-->
    <link rel="shortcut icon" type="image/png" href="/images/logo/logo.png" />
    <title>Let's Trade</title>
</head>

<body>
    <head-div class="header-info">
        <!-- storing all heading information-->
        <div class="logo">
            <a href="/"> <img alt='logo' src="/images/logo/logo.png" width="50" height="50"> </a>
            <div class="logo-info">
                <h4>Let's Trade</h4>
                <p>Buy . Sell . Rent</p>
            </div>
        </div>

        <nav class="navigation">
          

            <div class="auth-menus">
                <% if(!user){ %>
                <a href='/users/login'>Sign In</a>
                <p> | </p>
                <a href='/users/new'>Sign up</a>
                <% }else{ %>
                <a href='/users/profile'> Profile</a>
                <p> | </p>
                <a href='/users/logout'>Logout</a>
                <% } %>
            </div>

            <div class="nav-menu">
                <a href="/">Home</a>
                |
                <a href="/trade/trades"> Trade</a>
                |
                <a href="/trade/newTrade">Create Trade</a>
                |
                <a href="/main/about">about</a>
            </div>

        </nav>
    </head-div>

    <% if(errorMessages.length > 0) {%>
        <% errorMessages.forEach(message=>{%>
            <div class='error'>
                <%=message%>
            </div>
            <%});%>
                <%}%>
    
                    <% if(successMessages.length > 0) {%>
    
                        <% successMessages.forEach(message=>{%>
                            <div class='success'>
                                <%=message%>
                            </div>
                            <%});%>
                                <%}%>